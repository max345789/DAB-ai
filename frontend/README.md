# Frontend Service

The primary frontend service for DAB AI is the Next.js application at the repository root.

Relevant source directories:

- `src/app`
- `src/components`
- `src/lib`

This folder exists only as a service marker so the repository layout includes an explicit `frontend/` entry alongside `server/`.
