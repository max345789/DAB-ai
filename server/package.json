{
  "name": "dab-ai-server",
  "version": "1.0.0",
  "description": "DAB AI – Backend API server (Express + Supabase)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo 'Node.js server — no build step required'",
    "lint": "node --check index.js",
    "health": "curl -sf \"${HEALTHCHECK_URL:-https://api.dabcloud.in/api/health}\" || exit 1"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@supabase/supabase-js": "^2.49.4",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.0",
    "morgan": "^1.10.0",
    "node-cron": "^4.2.1",
    "openai": "^6.27.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
